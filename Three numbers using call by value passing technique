#include <stdio.h>
int big(int,int,int);
int main() {
int x,y,z,biggest;
scanf("%d %d %d",&x,&y,&z);
biggest=big(x,y,z);
    printf("%d is biggest",biggest);
    return 0;
}
int big(int a, int b, int c){
 if(a>b&&a>c){
     return a;
 }
 else if(b>c)
 return b;
 else
 return c;
}
